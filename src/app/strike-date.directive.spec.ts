import { StrikeDateDirective } from './strike-date.directive';

describe('StrikeDateDirective', () => {
  it('should create an instance', () => {
    const directive = new StrikeDateDirective();
    expect(directive).toBeTruthy();
  });
});
